function range(x, y) {
  results = [];

  for (i = x; i <= y; i++) {
    results.push(i);
  }
  return results;
}

function sum(range) {
  var count=0;
  for (var i = 0; i < results.length; i++) {
    count += results[i];
  }

  return count;
}
